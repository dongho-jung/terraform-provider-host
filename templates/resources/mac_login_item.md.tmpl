---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: ""
description: |-
  Manages one macOS Login Item by application bundle path.
---

# {{.Name}} ({{.Type}})

Manages one macOS Login Item by application bundle path.

Use this resource for GUI apps that should open at login. The provider uses macOS automation through `osascript`, adopts an existing login item with the same path, and removes the login item when the resource is destroyed.

Terraform must run as provider `target_user`, and that user must own the active macOS desktop session. If this resource blocks an apply for another or inactive user, log in as that user and run Terraform from a terminal in the desktop session without `sudo`. Use `terraform apply -target=<unaffected-resource-address>` only when specific unaffected resources must be staged first.

## Example Usage

{{ tffile "examples/resources/host_mac_login_item/resource.tf" }}

## Import

Import an existing Login Item by application bundle path:

```shell
terraform import host_mac_login_item.hammerspoon /Applications/Hammerspoon.app
```

{{ .SchemaMarkdown | trimspace }}
